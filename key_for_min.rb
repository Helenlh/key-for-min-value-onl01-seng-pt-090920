def key_for_min_value(name_hash)


def smallest_hash_value(hash) 
  hash = {:blake => 500, :ashley => 2, :adam => 1}
  key_for_min_value.min(1).key 
end 

 
    hash = {:blake => 500, :ashley => 2, :adam => 1}
    hash.each do (name, number)
    hash.min 
    together << number
    together.to_a 
end 
  together 
  
  
    

  #it "does not call the `#values` method" do
    hash = {:blake => 500, :ashley => 2, :adam => 1}
    hash = {:blake => 500, :ashley => 2, :adam => 1}
    hash.each (name, number)
    hash.min 
    together << name
    together.to_a 
  end



 # it "does not call the `#min method" do
    hash = {:blake => 500, :ashley => 2, :adam => 1}
def min_key_value_pair (test)
  if test == {}
       return nil
  else
       test = test.sort_by {|k, v| v}
       test[0]
   end
end
